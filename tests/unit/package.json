{
  "name": "truthguard-unit-tests",
  "version": "1.0.0",
  "description": "API & Unit Tests for TruthGuard — 300 Test Cases",
  "scripts": {
    "test": "jest --runInBand --forceExit --testTimeout=30000",
    "report": "node helpers/reporter.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
